org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dkotlin.daemon.jvm.options=-Xmx2048m
# AGP 8.9+ enables R8 full mode by default; it can strip plugin classes in release.
android.enableR8.fullMode=false
android.useAndroidX=true
android.enableJetifier=true
kotlin.incremental=false
# Keep false until all plugins drop android.kotlinOptions (KGP).
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
